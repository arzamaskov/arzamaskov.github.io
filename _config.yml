title: arzamaskov # name of the site
author: Andrey Arzamaskov # name of site's author
email: arzamaskov@gmail.com # email of site's author
url: https://arzamaskov.github.io # root address of the site
baseurl: "/" # subpath of the site, e.g. "/blog" (leave it blank "" if you're site shouldn't use a subpath)
description: > # description of the site (multiple lines allowed)
  personal blog.

permalink: /:slug.html

sass:
  style: :compressed

plugins:
  - jekyll-seo-tag

markdown: kramdown
kramdown:
  parse_block_html: true
